{
  "name": "${project.name}",
  "version": "1.0.0",
  "dependencies": {
    "cordova": "${version.cordova}",
    "ios-sim": "${version.ios-sim}",
    "ios-deploy": "${version.ios-deploy}"
  },
  "config": {
    "unsafe-perm": true
  },
  "scripts": {
    "prebuild": "npm install"
  }
}
